{
  "name": "archivebb-tests",
  "version": "0.0.1",
  "dependencies": {
    "jasmine": "3.1.0",
    "jasmine-reporters": "^2.3.1",
    "puppeteer": "1.4.0"
  },
  "scripts": {
    "jasmine": "node $NODE_DEBUG_OPTION ./node_modules/.bin/jasmine JASMINE_CONFIG_PATH=spec/support/jasmine.json",
    "build": "npm run",
    "pretest": "npm run build",
    "test": "npm run jasmine"
  }
}
